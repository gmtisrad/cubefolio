import { baseWrapperStyle } from '../styles';
import { BACKGROUND, TEXT, ACCENT_BACKGROUND } from '../constants/ColorConstants';

export const getCurrentStyle = () => baseWrapperStyle;

export const getBackgroundColor = () => BACKGROUND;

export const getTextColor = () => TEXT;

export const getAccentBackgroundColor = () => ACCENT_BACKGROUND;
